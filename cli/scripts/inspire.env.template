# Inspire CLI uses account and project config by default.
#
# Configure account, credentials, base URL, and proxy:
#   inspire account add <name>
#
# Bind a repository to an Inspire project and remote path:
#   inspire init
#
# Inspect the effective config:
#   inspire config show --compact
#   inspire config check
