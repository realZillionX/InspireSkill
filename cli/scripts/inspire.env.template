# Inspire CLI local environment template
#
# Use `inspire account add <name>` to provision per-account config; env vars
# below are useful for CI / scripts where committing credentials is not
# desirable. They override the active account's config.toml.

INSPIRE_PASSWORD=

# Optional: override the account's [paths].target_dir for the current shell.
INSPIRE_TARGET_DIR=
